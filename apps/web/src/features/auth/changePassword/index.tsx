import { FC, ReactElement } from 'react';
import ChangePassword from './pages/Page';


const UserChangePassword: FC = (): ReactElement => {
  return <ChangePassword />;
};

export default UserChangePassword;
